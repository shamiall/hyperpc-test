# Medusa Store API

Документация к кастомным API эндпоинтам вашего Medusa магазина.

## 🌐 Базовый URL

Все эндпоинты доступны относительно базового URL вашего сервера (по умолчанию `http://localhost:9000`).

## 💱 Currency API

Сервис для конвертации валют и получения информации о курсах.
Подробная документация доступна в [src/api/currency/README.md](./currency/README.md).

### Эндпоинты

| Метод | Путь | Описание |
|-------|------|----------|
| `GET` | `/currency/convert` | Конвертация суммы из одной валюты в другую |
| `GET` | `/currency/supported` | Список поддерживаемых валют |
| `GET` | `/currency/health` | Статус сервиса валют и кеша |

### Примеры использования

#### Конвертация валют

**Запрос:**
```bash
curl "http://localhost:9000/currency/convert?amount=100&from=USD&to=RUB"
```

**Ответ:**
```json
{
  "original": {
    "amount": 100,
    "currency": "USD"
  },
  "converted": {
    "amount": 9250.50,
    "currency": "RUB"
  },
  "exchange_rate": 92.505,
  "timestamp": "2024-03-20T12:00:00.000Z"
}
```

#### Получение списка валют

**Запрос:**
```bash
curl http://localhost:9000/currency/supported
```

## ⚙️ System API

Служебные эндпоинты для проверки работоспособности системы.

### Эндпоинты

| Метод | Путь | Описание |
|-------|------|----------|
| `GET` | `/ping` | Простая проверка доступности сервера |

### Примеры использования

#### Проверка здоровья (Ping)

**Запрос:**
```bash
curl http://localhost:9000/ping
```

**Ответ:**
```text
pong
```

## 🔐 Аутентификация

Эндпоинты в данной директории (`/api/*`) настроены следующим образом:
- **Currency API**: Публичный доступ (не требует `x-publishable-api-key` или токена администратора).
- **Ping**: Публичный доступ.

Если вы хотите защитить эти маршруты, вы можете добавить middleware в `src/api/middlewares.ts`.

## 💻 Примеры кода (Client Side)

### JavaScript (Fetch)

```javascript
// Функция конвертации
async function convertPrice(amount, from, to) {
  const response = await fetch(
    `http://localhost:9000/currency/convert?amount=${amount}&from=${from}&to=${to}`
  );
  return await response.json();
}

// Использование
convertPrice(50, 'EUR', 'USD').then(data => {
  console.log(`Converted: ${data.converted.amount} ${data.converted.currency}`);
});
```
